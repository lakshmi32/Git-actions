name: My First Workflow
on: push
jobs:
 first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome message
        run: echo "my first gitgub action job"
      - name: List files
        run: ls
      - name: Read file
        run: cat README.md
